# Copy to `.env` in this directory and fill in real values (`.env` is gitignored).
# Docker Compose loads `.env` automatically when you run `docker compose up` from the repo root.
LITMUS_EDGE_HOSTNAME=your-litmus-edge-hostname-or-ip
LITMUS_EDGE_TOKEN=your-api-token
